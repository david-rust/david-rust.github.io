{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "kcr-website",
      "runtimeExecutable": "docker",
      "runtimeArgs": ["run", "--rm", "--name", "kcr-preview", "-p", "3000:80", "-v", "${workspaceFolder}:/usr/share/nginx/html:ro", "-v", "${workspaceFolder}/nginx.conf:/etc/nginx/conf.d/default.conf:ro", "nginx:alpine"],
      "port": 3000
    }
  ]
}
