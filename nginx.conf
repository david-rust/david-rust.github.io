server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Keep redirects relative so they work on any port (dev or prod)
    absolute_redirect off;

    location / {
        try_files $uri $uri/ $uri.html =404;
    }
}
